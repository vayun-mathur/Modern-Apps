plugins {
    id("common-conventions-app")
}

android {
    defaultConfig {
        applicationId = "com.vayunmathur.dialer"
    }
    namespace = "com.vayunmathur.dialer"
}

dependencies {
    // Basic compose dependencies are provided by common-conventions-app
}

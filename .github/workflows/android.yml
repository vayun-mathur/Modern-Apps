name: "🚀 Auto-Increment & Release"

on:
  workflow_dispatch:

env:
  ks_filename: release_keystore.jks
  ks_store_pass: ${{ secrets.KS_STORE_PASS }}
  ks_alias: ${{ secrets.KS_ALIAS }}
  ks_alias_pass: ${{ secrets.KS_ALIAS_PASS }}

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set Up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'
          cache: 'gradle'

      - name: Clean settings.gradle.kts
        run: |
          if [ -f "settings.gradle.kts" ]; then
            echo "Removing local includeBuild from settings.gradle.kts for CI..."
            # More robust sed to delete the multiline block starting with includeBuild and ending at the next top-level '}'
            sed -i '/includeBuild.*NewPipeExtractor/,/^}/d' settings.gradle.kts
            echo "Resulting settings.gradle.kts:"
            cat settings.gradle.kts
          fi

      - name: Decode Keystore
        run: echo "${{ secrets.KEYSTORE_FILE }}" | base64 --decode > ${{ env.ks_filename }}

      - name: Change Gradle Wrapper Permissions
        run: chmod +x ./gradlew

      # 1. READ version.txt AND INJECT into build.gradle.kts
      - name: Read and Inject Version
        id: version_info
        run: |
          VERSION_FILE="version.txt"
          
          if [ ! -f "$VERSION_FILE" ]; then
            echo "Error: $VERSION_FILE not found!"
            exit 1
          fi

          # Read values and strictly strip ALL whitespace and carriage returns
          VERSION_CODE=$(sed -n '1p' "$VERSION_FILE" | tr -d '\r' | xargs)
          VERSION_NAME=$(sed -n '2p' "$VERSION_FILE" | tr -d '\r' | xargs)

          echo "Using Version Name: $VERSION_NAME"
          echo "Using Version Code: $VERSION_CODE"
          
          # 2. INJECT variables into the defaultConfig block of MODULES only
          find . -mindepth 2 -name "build.gradle.kts" | while read file; do
            echo "Processing module file: $file..."
            sed -i '/versionCode[[:space:]]*=[[:space:]]*/d' "$file"
            sed -i '/versionName[[:space:]]*=[[:space:]]*/d' "$file"
            sed -i "/defaultConfig[[:space:]]*{/a \        versionCode = $VERSION_CODE\n        versionName = \"$VERSION_NAME\"" "$file"
          done

          echo "version_name=$VERSION_NAME" >> $GITHUB_OUTPUT
          echo "version_code=$VERSION_CODE" >> $GITHUB_OUTPUT

      # 3. COMMIT, TAG & PUSH (TAG ONLY)
      - name: Commit, Tag & Push Changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add .
          
          TAG_NAME="${{ steps.version_info.outputs.version_name }}"
          git commit -m "chore: prepare release $TAG_NAME (${{ steps.version_info.outputs.version_code }})" || echo "No changes to commit"
          
          git tag -f "$TAG_NAME"
          git push origin "$TAG_NAME" --force

      # 4. DETECTION (Handling Nested Modules)
      - name: Detect App Modules
        run: |
          # Finds directories relative to root (e.g. 'app' or 'games/chess')
          # 1. Find all build.gradle.kts with versionCode
          # 2. Exclude root and youpipe
          # 3. Strip leading './' and trailing '/build.gradle.kts'
          MODULE_DIRS=$(grep -rl "versionCode" . --include="build.gradle.kts" | grep -vE "^\./build.gradle.kts$|youpipe" | sed 's|^\./||; s|/build.gradle.kts$||' | sort -u | tr '\n' ' ')
          echo "Detected module directories: $MODULE_DIRS"
          echo "module_dirs=$MODULE_DIRS" >> $GITHUB_ENV

      - name: Download Bundletool
        run: |
          mkdir -p .github/lib
          curl -L https://github.com/google/bundletool/releases/download/1.17.2/bundletool-all-1.17.2.jar -o .github/lib/bundletool.jar

      # 5. BUILD & SIGN
      - name: Build and Sign
        run: |
          mkdir -p distribution_apks
          for dir in ${{ env.module_dirs }}; do
            # Convert directory path to Gradle colon path (e.g. games/chess -> :games:chess)
            gradle_path=$(echo $dir | sed 's|/|:|g')
            # Extract the leaf name (e.g. games/chess -> chess)
            module_name=$(basename $dir)
          
            echo "--------------------------------------------"
            echo "Building: $gradle_path (Dir: $dir, Name: $module_name)"
            echo "--------------------------------------------"
          
            ./gradlew :$gradle_path:bundleRelease
          
            java -jar ".github/lib/bundletool.jar" build-apks \
              --bundle=$dir/build/outputs/bundle/release/$module_name-release.aab \
              --output=$dir/build/outputs/bundle/release/$module_name.apks \
              --mode=universal \
              --ks="${{ env.ks_filename }}" \
              --ks-pass=pass:${{ env.ks_store_pass }} \
              --ks-key-alias=${{ env.ks_alias }} \
              --key-pass=pass:${{ env.ks_alias_pass }}
          
            unzip -p $dir/build/outputs/bundle/release/$module_name.apks universal.apk > distribution_apks/$module_name-release.apk
          done

      # 6. CREATE RELEASE
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: "${{ steps.version_info.outputs.version_name }}"
          name: "Release ${{ steps.version_info.outputs.version_name }}"
          body: |
            Automated manual release.
            Version Name: ${{ steps.version_info.outputs.version_name }}
            Version Code: ${{ steps.version_info.outputs.version_code }}
          files: distribution_apks/*.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}